#!/data/data/com.termux/files/usr/bin/bash
# ai = thin entrypoint for the installed runtime.
set -euo pipefail

LIB="${AI_LIB_DIR:-$HOME/.config/ai/lib}"
if [ ! -f "$LIB/ai_cli.py" ]; then
  echo "ERROR: ai runtime is not installed: $LIB" >&2
  exit 127
fi

exec python "$LIB/ai_cli.py" "$@"
